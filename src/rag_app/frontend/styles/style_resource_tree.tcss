.resource-tree {
    width: auto;
    min-width: 40;
    height: auto;         
    max-height: 50vh;       
    background: transparent;
    padding: 0 1;
}

.resource-tree > .tree--label {
    color: $text;
}
